import "./style.scss";
//import { MapContainer, TileLayer, Marker, Popup } from "react-leaflet";

const Map = () => {
  return (
    <div className="map-wrapper" >
    </div>
  );
};

export default Map;
